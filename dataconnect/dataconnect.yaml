specVersion: "v1"
serviceId: "todo-service"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todo-service-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
